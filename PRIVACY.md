# Privacy Policy / プライバシーポリシー

最終更新日: 2026年9月15日

## 1. NaviComについて

NaviComは、VS Code上で動作する学習支援AIナビゲーターです。質問や作業中のコードをもとに、考え方・確認ポイント・次に見る箇所などの助言を生成します。

## 2. NaviComが利用する情報

AIへの問い合わせには、利用する機能・推論強度・除外設定・モデルの入力上限に応じて、次の情報が含まれます。毎回すべてを送るわけではありません。

- 入力した質問・追加コンテキスト、選択した相談機能・助言モード・推論強度。
- ワークスペース内の選択中のコード、アクティブファイルの抜粋、使用言語、ファイル名・パス。短いファイルでは抜粋が全文に相当する場合があります。
- エラー・警告などの診断メッセージ、重大度、発生元、行番号。
- 直近の編集箇所と変更前後・追加・削除されたテキストの抜粋、現在位置から取得した関数名・変数名などの関連シンボル候補。
- 関連ファイルの抜粋・診断・編集情報、そのファイルを選んだ理由・スコア、ワークスペースのファイル・ディレクトリ構造。
- プロジェクトの次の作業を相談する機能では、開いているファイルの一覧、ワークスペース全体の診断・編集の要約、TODO/FIXME/XXXの記述、設定・マニフェストの抜粋やパッケージ名・スクリプト名・依存パッケージ名、READMEやdocsの見出し・冒頭部分などの要約。
- 常時モードでは、自動助言のきっかけ、操作が止まっていた時間、カーソルの行・列と周辺コード、選択の有無・行数、編集量・カーソルとの距離、診断の追加・解消・残存状況、直前の助言の焦点・概要説明済みかどうか。
- 再利用対象として選ばれた有効な個人ナレッジのタイトル・要約。必要時の相談では、評価理由からローカルで生成した定型のフィードバック傾向も含まれます。補足コメントや回答抜粋を、この傾向生成のためにAIへ送ることはありません。

通常の相談では過去の会話本文を自動で付け足しません。ただし、その相談に保存した追加コンテキストは次回も利用します。ナレッジ下書きの作成を依頼した場合は、対象回答、その周辺の最大7件のメッセージの抜粋・日時・役割・相談種別、およびその時点で利用できるコード抜粋・診断・編集・関連ファイル・追加コンテキスト・参照カテゴリなどを、作成時に接続中のAIへ送ります。会話タイトルはローカルで生成します。

送信前に「送信予定」でカテゴリ・対象ファイル・概算サイズを確認できます。必要時モードでは質問操作により、常時モードでは編集・選択・診断の変化などに応じて自動で問い合わせます。自動送信を止めるには一時停止するか、必要時モードに切り替えてください。信頼されていないワークスペースではAI接続や作業文脈の収集を制限します。

NaviComが構造化して扱うファイルパスは、送信前にワークスペース相対パスなどへ変換します。複数ルートの場合はルート名も含まれます。コード本文・診断メッセージ・自由入力に書かれた絶対パスや秘密情報を一律に除去する機能ではありません。

`.env`や秘密鍵などの保護用除外パターンと、ユーザーが追加した除外パターンに一致するファイル本文等は、相談の送信対象から除外します。ただし、除外されたアクティブファイルの名前・言語が含まれる場合があります。自由入力や保存済みの会話・ナレッジに含まれる情報にも、このファイル除外が自動で適用されるわけではありません。

## 3. AIプロバイダーへのデータ送信

ユーザーは接続画面・設定画面からプロバイダーと利用モデルを選択できます。各接続先には、上記の機能別の入力と、NaviComの応答指示・モデル指定・生成上限などが渡されます。応答形式の修正や一部の通信エラーに対して再送する場合があります。

### GitHub Copilot / VS Code Language Model API

`vscode.lm.selectChatModels({ vendor: "copilot" })`でモデルを取得し、Language Model APIの`sendRequest`へプロンプト・コンテキストを渡します。実際の通信先はVS CodeおよびCopilot側の実装・選択モデルに依存し、NaviComは通信先URLを指定しません。接続確認では短い定型文を送ります。利用量の推計のため、同APIの`countTokens`にプロンプトや応答本文を渡す場合もあります。

GitHub、Microsoftやモデル提供者によるデータ保持・学習利用の条件を、NaviComが保証するものではありません。

### OrcaRouter

OrcaRouterを選択すると、プロンプトと必要なコンテキストを`https://api.orcarouter.ai/v1/chat/completions`へ送信します。モデル一覧は同じサービスの`/v1/models`から取得します。両方の通信でAPIキーを認証に使用します。推論には選択したモデルIDを指定し、OrcaRouter側でルーティングされたモデル提供者も処理に関与します。

OrcaRouterや上流モデル提供者での保存期間・学習利用・再転送などは、NaviComのコードでは管理していません。利用するサービス自身のプライバシーポリシー・利用規約を確認してください。

### LM Studio

ユーザーが設定したLM Studioサーバーへ送ります。現在の実装はHTTP(S)の`localhost`、`127.0.0.1`、`::1`のみを許可し、既定値は`http://127.0.0.1:1234`です。これは拡張機能ホストから見た接続先です。

モデル一覧・状態確認には`/api/v1/models`、生成には`/v1/chat/completions`、推論設定を指定する経路では`/api/v1/chat`を使います。Chat Completionsの経路では参照ファイルのパス一覧を`navicom_referenced_files`として付ける場合もあります。`/api/v1/chat`の経路では`store: false`と空の`integrations`を指定しますが、サーバー全体の保存動作を保証するものではありません。

ローカルで実行している場合、NaviComからクラウドAIサービスへ直接送信しません。ただし、サーバー自身の通信やポート転送などの構成まで保証できないため、「LM Studioなら必ず外部送信されない」とは限りません。

### Ollama

ユーザーが設定したHTTP(S)サーバーへ送ります。既定値は`http://localhost:11434`ですが、別ホストも指定できます。モデル一覧は`/api/tags`、生成は`/v1/chat/completions`を使い、プロンプト・コンテキストと参照ファイルのパス一覧を送る場合があります。接続先切り替え時には、モデルを解放するため`/api/generate`へモデル名と`keep_alive: 0`などを送ります。

送信範囲はサーバーの配置・構成に依存します。サーバー自身やモデルに関わる外部サービスの処理についても確認してください。

## 4. ローカルに保存されるデータ

NaviComは次のデータをVS Codeが提供する拡張機能用の保存領域へ保存します。SQLiteにNaviCom独自の暗号化は施していません。

| データ | 保存内容・目的 | 保存場所 |
| --- | --- | --- |
| 会話履歴 | 質問・回答・日時・タイトル・追加コンテキスト、ファイル名・選択箇所のプレビュー・診断・送信計画、助言設定・焦点・評価、プロバイダー・モデル、トークン量・料金・応答ID等。再閲覧・相談再開に利用 | ワークスペース別の`ExtensionContext.storageUri`内の`conversations.sqlite`。保存領域がない場合は`globalStorageUri/workspace-history/conversations.sqlite` |
| ナレッジ | タイトル・要約・本文・有効状態・元回答ID・モデル情報・日時・検索用テキスト。検索と助言への再利用に使用 | `ExtensionContext.globalStorageUri`内の`knowledge.sqlite`（ワークスペース共通） |
| フィードバック | 回答ID、Good/Bad、選択理由、補足コメント、相談種別・推論強度・コマンド、日時。評価の保持と定型傾向の生成に使用 | 同じグローバル保存領域の`feedback.sqlite` |
| 設定 | 接続先・モデル・サーバーURL、既定モード・推論強度、自動助言の間隔、日次トークン上限、除外パターン | VS Codeの`workspaceState`（キー`aiPairNavigator.phase2.settings`）。Copilot接続確認の待ち時間は通常のVS Code設定 |
| 利用量 | 日付、プロバイダー・モデル別のリクエスト数、入力・出力トークン数、取得できた料金。利用量表示・上限制御に使用 | VS Codeの`globalState`（キー`aiPairNavigator.usage.daily`） |
| 診断ログ | 起動時の拡張機能パス、モデル、処理時間、トークン数、応答ID・終了理由・エラー分類、常時モードの判断結果、一部の入力のハッシュ値・文字数など | VS Codeの「NaviCom Diagnostics」出力・ログ。保存失敗・接続失敗等はExtension Hostログにも記録 |

送信文脈全体は現在の会話保存処理ではSQLiteに書き込まず、実行中のメモリで保持します。ただし、上表の選択箇所プレビュー・診断・追加コンテキストや、質問・回答に含まれるコードは保存されます。直近編集の取得用スナップショットなどもメモリで扱います。

保存場所の実際のパスはOS、VS Codeのプロファイル、開発用設定や実行環境によって異なります。「ローカル保存」は拡張機能ホスト側の保存を指し、リモート環境では手元のPCとは異なる場合があります。

これらのデータベースや利用量集計・ログを、NaviComが開発者のサーバーへアップロードする処理はありません。ただし、追加コンテキスト、再利用ナレッジのタイトル・要約、定型フィードバック傾向、ナレッジ作成時の対象会話・参照文脈は、前述のとおりAI入力として送信します。ナレッジとフィードバックはワークスペース共通なので、別のワークスペースや接続先で利用される場合があります。

### 削除方法と制限

- 履歴画面で相談を個別に、または「すべて削除」で全削除できます。対応するフィードバックも削除対象になります。会話は最大100件の相談を保持し、古い相談と関連フィードバックを整理します。
- ナレッジはナレッジ画面から個別に削除してください。元の会話を削除しても、ナレッジは別途残ります。
- NaviComの設定は設定画面の「初期値に戻す」でリセットできます。会話・ナレッジ・APIキーの削除とは別の操作です。Copilot接続確認の待ち時間はVS Code設定でリセットできます。
- 日次利用量を削除する専用UIはありません。日付が変わると集計対象が切り替わりますが、直ちに保存値が消去されるという意味ではありません。

SQLiteの更新時は直前のファイルを同じ場所の`*.sqlite.bak`に残します。異常終了時には`*.sqlite.<プロセスID>.<時刻>.tmp`が残る場合もあります。画面からの削除は論理削除であり、バックアップやSQLite内の削除済み領域の安全な消去は実装していません。

保存ファイルごと取り除く場合は、NaviComが動作するVS Codeを終了したうえで、上記のNaviCom用保存領域にある対象のSQLite本体・`.bak`・残存する`.tmp`を削除してください。本体だけを削除するとバックアップから復元される場合があります。設定・利用量はVS Code管理の状態データ内にあり、このファイル削除では消えません。これらやログまで除去する場合は対象プロファイルの保存先を確認し、NaviComの状態・ログだけを対象にしてください。他の拡張機能も使う状態データベース全体は削除しないでください。

AIプロバイダーへ既に送信されたデータは、ローカルで削除しても送信先から削除されません。

## 5. APIキー・認証情報

OrcaRouterのAPIキーは、設定画面からVS Codeの`SecretStorage`へ保存します（キー名`aiPairNavigator.orcaRouter.apiKey`）。保存済みの値は画面へ再表示せず、設定済みかどうかのみを通知します。モデル一覧取得・推論時に取り出し、OrcaRouterへの`Authorization: Bearer`ヘッダーに使用します。設定画面の「キーを削除」でSecretStorageから削除できます。

この認証経路ではAPIキーをSQLite・通常の設定へ平文保存せず、プロンプト・会話履歴・通常のログへ記録する処理もありません。Copilotの認証・認可はVS CodeとCopilotに委ね、NaviComはGitHubのパスワードやトークンを独自に保存しません。LM Studio・Ollama用のAPIキー保存や認証ヘッダーの送信は実装していません。

ただし、利用者自身が質問・追加コンテキスト・コード・フィードバックの補足などに秘密情報を書き込んだ場合、一般の入力データとして保存・送信される可能性があります。任意の本文や第三者の応答を対象に、秘密情報を完全に検出・除去する仕組みではありません。

## 6. テレメトリ

NaviCom独自の外部テレメトリ、アナリティクス、クラッシュレポート、利用状況の自動送信は実装していません。第4節の利用量集計と診断ログは拡張機能ホスト側で扱います。

開発者向けのライブ評価コマンドを明示的に実行した場合は、同梱のテスト用プロンプトを接続中のモデルへ送り、評価結果を「NaviCom Eval」出力へ表示します。通常の質問操作でこの評価は実行しません。

VS Code、GitHub Copilot、OrcaRouter、LM Studio、Ollamaや各モデル提供者自身が行う情報収集・テレメトリは、NaviComの実装とは別です。

## 7. 第三者サービス

各AIプロバイダーやモデル提供者には、それぞれの利用規約・プライバシーポリシーが適用されます。送信先での保持期間、学習利用、削除方法などは利用者の契約・設定も含めて各サービスで確認してください。NaviComは第三者サービスの取り扱いを保証しません。

## 8. プライバシーポリシーの変更

機能追加やデータの取り扱いの変更などに伴い、本ポリシーを更新する場合があります。最新版は本リポジトリの`PRIVACY.md`で公開します。

## 9. 問い合わせ

[NaviComのGitHubリポジトリ](https://github.com/y4m4s/vol2-project)で案内される窓口をご利用ください。問い合わせ時には、APIキーや非公開コードなどの秘密情報を公開しないでください。
