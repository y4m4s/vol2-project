[
  {"id":"bug-boundary","category":"code_reasoning","code":"function sum(xs) { let total = 0; for (let i = 0; i <= xs.length; i++) total += xs[i]; return total; }","question":"合計がNaNになる理由を、着目する箇所と確認方法だけで教えて。完成コードは不要。","expected":"i <= xs.length reaches xs[xs.length] = undefined; adding yields NaN. Point to last iteration, no replacement expression.","noCode":true},
  {"id":"explain-reduce","category":"explanation","code":"const total = prices.reduce((sum, price) => sum + price, 0);","question":"この処理と末尾の0の意味を2文以内で説明して。","expected":"Sums prices; 0 initial accumulator and empty-array result. Japanese <=2 sentences.","maxChars":240},
  {"id":"learn-next","category":"pedagogy","code":"const adults = users.filter(user => user.age >= 18);","question":"filterは理解できました。次に学ぶことを1つだけ、今のコードにつながる小さな練習付きで教えて。","expected":"One next topic such as map with a small exercise using adults; no finished code.","noCode":true,"maxChars":380},
  {"id":"hint-only","category":"instruction","code":"async function load() { const response = fetch('/users'); return response.json(); }","question":"答えや修正コードを出さず、ヒントを1つだけください。","expected":"Direct attention to value/type returned by fetch and when response is available; do not reveal await as the fix.","forbidden":["await fetch","awaitを追加","await を追加"],"noCode":true,"maxChars":240},
  {"id":"compare-approaches","category":"comparison","code":"const ids = ['a', 'b', 'a'];","question":"重複を消すのにSetとfilterを使う方針を、順序と計算量の観点で簡潔に比較して。コードは不要。","expected":"Set preserves insertion order; filter+indexOf preserves first occurrence but O(n²), Set usually O(n) expected, not absolute guarantee for all implementations.","noCode":true},
  {"id":"additional-requirement","category":"additional_context","code":"print('unrelated')","additional":"課題: 整数Nを受け取り、1からNまでの奇数を1行に1個ずつ表示する。N自身が奇数なら含める。","question":"この問題の出力形式と、Nが5のときに表示する値を教えて。","expected":"1,3,5 on separate lines; explain requirement directly, no advice about unrelated editor code.","maxChars":280},
  {"id":"uncertain-external","category":"uncertainty","code":"const result = await fetchData(userId); render(result);","question":"これが遅い原因はDBのインデックス不足ですか？","expected":"Cannot determine; fetchData implementation and timing/network/query evidence needed. No invented database or endpoint.","noCode":true},
  {"id":"long-active-tail","category":"long_context","fixture":"long-active","question":"最後のparsePortの戻り値の型と、空文字を渡したときの値を説明して。","expected":"parsePort returns Number(value): number, empty string produces 0; if code absent acknowledge unavailable, but pipeline context utilization suffers.","evidence":"return Number(value)"},
  {"id":"long-additional-tail","category":"long_context","fixture":"long-additional","code":"const timeout = 30;","question":"追加コンテキスト末尾の最新仕様ではタイムアウトは何秒ですか？古いメモと区別して答えて。","expected":"Latest spec is 17 seconds. Earlier 30 is obsolete. If latest missing do not guess.","evidence":"最新仕様: タイムアウトは17秒"},
  {"id":"related-file","category":"cross_file","depth":"high","code":"import { limit } from './config'; export const allowed = n => n <= limit;","references":[{"path":"config.ts","excerpt":"export const limit = 12;"}],"question":"allowed(12)とallowed(13)はどうなりますか。参照した定義も示して。","expected":"true and false; cite config.ts limit=12.","maxChars":300},
  {"id":"history-absent","category":"history","code":"const enabled = true;","question":"さっきの案と比べて、どちらがいいですか？","expected":"Previous proposal absent; ask for its contents, do not fabricate history.","maxChars":260},
  {"id":"auto-complete","category":"automatic","existing":"task-completion-layout-h-h","expected":"Correctly output five squares horizontally; no_advice focus none."},
  {"id":"auto-layout","category":"automatic","existing":"task-completion-layout-v-h","expected":"Current output horizontal, requirement vertical; one discovery hint without replacement code."}
]
